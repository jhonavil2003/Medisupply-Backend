# Database Configuration
POSTGRES_USER=salesuser
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=sales_db
POSTGRES_PORT=5434

# Service Configuration
FLASK_ENV=development
SERVICE_PORT=3003

# External Services
CATALOG_SERVICE_URL=http://localhost:3001
LOGISTICS_SERVICE_URL=http://localhost:3002
EXTERNAL_SERVICE_TIMEOUT=3

# Database URL (constructed from above values)
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}

# Security Note:
# 1. Copy this file to .env and set your actual credentials
# 2. NEVER commit .env to version control
# 3. Use strong passwords in production environments
# 4. Update CATALOG_SERVICE_URL and LOGISTICS_SERVICE_URL for production
